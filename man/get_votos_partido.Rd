% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resultados.R
\name{get_votos_partido}
\alias{get_votos_partido}
\title{List per-party votes (cross-election)}
\usage{
get_votos_partido(
  year = NULL,
  tipo_eleccion = NULL,
  tipo_territorio = NULL,
  codigo_ccaa = NULL,
  codigo_provincia = NULL,
  codigo_municipio = NULL,
  ...,
  eleccion_id = NULL,
  territorio_id = NULL,
  partido_id = NULL,
  limit = 50L,
  skip = 0L,
  all_pages = FALSE,
  denormalize = FALSE,
  use_recode = FALSE,
  clean = denormalize,
  api_key = NULL
)
}
\arguments{
\item{year}{Character vector. Filter by year(s). Optional.}

\item{tipo_eleccion}{Character vector. Filter by election type code(s).
Valid values: \code{"A"} (Autonomicas), \code{"E"} (Europeas), \code{"G"} (Congreso),
\code{"L"} (Locales), \code{"S"} (Senado). Optional.}

\item{tipo_territorio}{Character vector. Filter by territory type(s).
Valid values: \code{"ccaa"}, \code{"provincia"}, \code{"municipio"}, \code{"distrito"},
\code{"seccion"}, \code{"circunscripcion"}, \code{"cera"}. Optional.}

\item{codigo_ccaa}{Character vector. Filter by autonomous community INE
code(s) (e.g. \code{"01"}, \code{"13"}). Optional.}

\item{codigo_provincia}{Character vector. Filter by province INE code(s)
(e.g. \code{"28"}, \code{"08"}). Optional.}

\item{codigo_municipio}{Character vector. Filter by municipality INE
code(s). Optional.}

\item{...}{Arguments after \code{...} must be named.}

\item{eleccion_id}{Integer vector. Filter by election ID(s). Optional.}

\item{territorio_id}{Integer vector. Filter by territory ID(s). Optional.}

\item{partido_id}{Integer vector. Filter by party ID(s). Optional.}

\item{limit}{Integer. Maximum records per page (1-500, default 50).}

\item{skip}{Integer. Records to skip (default 0).}

\item{all_pages}{Logical. If \code{TRUE}, fetches all pages. Default \code{FALSE}.}

\item{denormalize}{Logical. If \code{TRUE}, adds descriptive columns next to
ID columns: \code{eleccion_descripcion} (after \code{eleccion_id}),
\code{territorio_nombre} (after \code{territorio_id}), and \code{partido_nombre}
(after \code{partido_id}). Requires additional API calls. Default \code{FALSE}.}

\item{use_recode}{Logical. If \code{TRUE} and \code{denormalize = TRUE}, the
\code{partido_nombre} column uses the recode group name (\code{agrupacion})
instead of the party abbreviation (\code{siglas}). Falls back to \code{siglas}
when the party has no recode. Default \code{FALSE}.}

\item{clean}{Logical. If \code{TRUE}, removes ID and slug columns from the
result. Defaults to the value of \code{denormalize}.}

\item{api_key}{(Opcional) Clave de API para sobrescribir la global solo en esta llamada.}
}
\value{
A tibble with columns: \code{id}, \code{eleccion_id}, \code{territorio_id},
\code{partido_id}, \code{votos}, \code{representantes}. When \code{denormalize = TRUE},
additional columns \code{eleccion_descripcion}, \code{territorio_nombre}, and
\code{partido_nombre} are inserted after their corresponding ID columns.
When \code{clean = TRUE}, ID and slug columns are removed.
}
\description{
Returns paginated per-party vote records with flexible filters.
}
\examples{
\dontrun{
# Votes in general elections of 2019
get_votos_partido(
    year = "2019", tipo_eleccion = "G",
    tipo_territorio = "provincia"
)

# With denormalized names using recode grouping
get_votos_partido(
    year = "2019", tipo_eleccion = "G",
    tipo_territorio = "provincia",
    denormalize = TRUE, use_recode = TRUE, all_pages = TRUE
)
}
}
