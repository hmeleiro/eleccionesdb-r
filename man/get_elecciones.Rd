% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/elecciones.R
\name{get_elecciones}
\alias{get_elecciones}
\title{List elections with optional filters}
\usage{
get_elecciones(
  tipo_eleccion = NULL,
  year = NULL,
  mes = NULL,
  ambito = NULL,
  limit = 50L,
  skip = 0L,
  all_pages = FALSE
)
}
\arguments{
\item{tipo_eleccion}{Character vector. Filter by election type code(s).
Valid values: \code{"A"} (Autonomicas), \code{"E"} (Europeas), \code{"G"} (Congreso),
\code{"L"} (Locales), \code{"S"} (Senado). Optional.}

\item{year}{Character vector. Filter by year (e.g. \code{"2019"},
\code{c("2019", "2023")}). Optional.}

\item{mes}{Character vector. Filter by month with leading zero
(e.g. \code{"04"}, \code{"11"}). Optional.}

\item{ambito}{Character vector. Filter by scope (e.g. \code{"Nacional"},
\code{"Autonomico"}). Optional.}

\item{limit}{Integer. Maximum records to return per page (1-500, default 50).}

\item{skip}{Integer. Records to skip (default 0).}

\item{all_pages}{Logical. If \code{TRUE}, fetches all pages automatically.
Default \code{FALSE}.}
}
\value{
A tibble with columns: \code{id}, \code{tipo_eleccion}, \code{year}, \code{mes}, \code{dia},
\code{fecha} (Date), \code{descripcion}, \code{ambito}, \code{slug}.

The tibble has an \code{"edb_total"} attribute with the total record count.
}
\description{
Returns a paginated list of elections. Use \code{all_pages = TRUE} to retrieve
all matching records automatically.
}
\examples{
\dontrun{
# All general elections
get_elecciones(tipo_eleccion = "G")

# General elections in 2019
get_elecciones(tipo_eleccion = "G", year = "2019")

# Fetch all elections (all pages)
get_elecciones(all_pages = TRUE)
}
}
