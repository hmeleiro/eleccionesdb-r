% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/territorios.R
\name{get_territorios}
\alias{get_territorios}
\title{List territories with optional filters}
\usage{
get_territorios(
  tipo = NULL,
  codigo_ccaa = NULL,
  codigo_provincia = NULL,
  codigo_municipio = NULL,
  codigo_circunscripcion = NULL,
  nombre = NULL,
  limit = 50L,
  skip = 0L,
  all_pages = FALSE
)
}
\arguments{
\item{tipo}{Character vector. Filter by territory type
(e.g. \code{"ccaa"}, \code{"provincia"}, \code{"municipio"}). Optional.}

\item{codigo_ccaa}{Character vector. Filter by autonomous community code
(e.g. \code{"01"}, \code{"13"}). Optional.}

\item{codigo_provincia}{Character vector. Filter by province code
(e.g. \code{"28"}, \code{"08"}). Optional.}

\item{codigo_municipio}{Character vector. Filter by municipality code. Optional.}

\item{codigo_circunscripcion}{Character vector. Filter by constituency code. Optional.}

\item{nombre}{Character. Partial name search, case-insensitive
(e.g. \code{"madrid"}). Optional.}

\item{limit}{Integer. Maximum records per page (1-500, default 50).}

\item{skip}{Integer. Records to skip (default 0).}

\item{all_pages}{Logical. If \code{TRUE}, fetches all pages. Default \code{FALSE}.}
}
\value{
A tibble with columns: \code{id}, \code{tipo}, \code{nombre}, \code{codigo_completo},
\code{codigo_ccaa}, \code{codigo_provincia}.
}
\description{
Returns a paginated list of territories. Supports filtering by type,
administrative codes, and partial name search.
}
\examples{
\dontrun{
# All autonomous communities
get_territorios(tipo = "ccaa")

# Search by name
get_territorios(nombre = "madrid", tipo = "provincia")

# Provinces in Andalucia (codigo_ccaa = "01")
get_territorios(tipo = "provincia", codigo_ccaa = "01", all_pages = TRUE)
}
}
