% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resultados.R
\name{get_totales_territorio_eleccion}
\alias{get_totales_territorio_eleccion}
\title{List territorial totals for an election}
\usage{
get_totales_territorio_eleccion(
  eleccion_id,
  tipo_territorio = NULL,
  codigo_ccaa = NULL,
  codigo_provincia = NULL,
  codigo_municipio = NULL,
  ...,
  territorio_id = NULL,
  limit = 50L,
  skip = 0L,
  all_pages = FALSE,
  denormalize = FALSE,
  clean = denormalize
)
}
\arguments{
\item{eleccion_id}{Integer. The election ID (required).}

\item{tipo_territorio}{Character vector. Filter by territory type.
Valid values: \code{"ccaa"}, \code{"provincia"}, \code{"municipio"}, \code{"distrito"},
\code{"seccion"}, \code{"circunscripcion"}, \code{"cera"}. Optional.}

\item{codigo_ccaa}{Character vector. Filter by autonomous community INE
code(s) (e.g. \code{"01"}, \code{"13"}). Optional.}

\item{codigo_provincia}{Character vector. Filter by province INE code(s)
(e.g. \code{"28"}, \code{"08"}). Optional.}

\item{codigo_municipio}{Character vector. Filter by municipality INE
code(s). Optional.}

\item{...}{Arguments after \code{...} must be named.}

\item{territorio_id}{Integer vector. Filter by territory ID(s). Optional.}

\item{limit}{Integer. Maximum records per page (1-500, default 50).}

\item{skip}{Integer. Records to skip (default 0).}

\item{all_pages}{Logical. If \code{TRUE}, fetches all pages. Default \code{FALSE}.}

\item{denormalize}{Logical. If \code{TRUE}, adds descriptive columns next to
ID columns: \code{eleccion_descripcion} (after \code{eleccion_id}) and
\code{territorio_nombre} (after \code{territorio_id}). Requires additional API
calls. Default \code{FALSE}.}

\item{clean}{Logical. If \code{TRUE}, removes ID and slug columns from the
result. Defaults to the value of \code{denormalize}.}
}
\value{
A tibble with columns: \code{id}, \code{eleccion_id}, \code{territorio_id},
\code{censo_ine}, \code{participacion_1}, \code{participacion_2}, \code{participacion_3},
\code{votos_validos}, \code{abstenciones}, \code{votos_blancos}, \code{votos_nulos},
\code{nrepresentantes}. When \code{denormalize = TRUE}, additional columns
\code{eleccion_descripcion} and \code{territorio_nombre} are inserted after
their corresponding ID columns. When \code{clean = TRUE}, ID and slug
columns are removed.
}
\description{
Returns paginated territorial totals (census, participation, blank/null
votes, etc.) for a specific election, with optional territorial filters.
}
\examples{
\dontrun{
# Totals for a general election, by province
get_totales_territorio_eleccion(208, tipo_territorio = "provincia")

# All totals for an election
get_totales_territorio_eleccion(208, all_pages = TRUE)

# With denormalized names (clean by default)
get_totales_territorio_eleccion(208,
    tipo_territorio = "provincia",
    denormalize = TRUE
)
}
}
