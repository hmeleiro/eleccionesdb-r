% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/partidos.R
\name{get_partidos}
\alias{get_partidos}
\title{List political parties with optional filters}
\usage{
get_partidos(
  siglas = NULL,
  denominacion = NULL,
  partido_recode_id = NULL,
  limit = 50L,
  skip = 0L,
  all_pages = FALSE,
  api_key = NULL
)
}
\arguments{
\item{siglas}{Character. Partial search by party abbreviation,
case-insensitive (e.g. \code{"psoe"}). Optional.}

\item{denominacion}{Character. Partial search by full party name,
case-insensitive (e.g. \code{"socialista"}). Optional.}

\item{partido_recode_id}{Integer vector. Filter by recode/group ID(s). Optional.}

\item{limit}{Integer. Maximum records per page (1-500, default 50).}

\item{skip}{Integer. Records to skip (default 0).}

\item{all_pages}{Logical. If \code{TRUE}, fetches all pages. Default \code{FALSE}.}

\item{api_key}{(Opcional) Clave de API para sobrescribir la global solo en esta llamada.}
}
\value{
A tibble with columns: \code{id}, \code{siglas}, \code{denominacion},
\code{partido_recode_id}.
}
\description{
Returns a paginated list of parties. Supports partial search by
abbreviation (\code{siglas}) and full name (\code{denominacion}), and filtering
by recode group.
}
\examples{
\dontrun{
# Search for PSOE-related parties
get_partidos(siglas = "psoe")

# All parties in a specific recode group
get_partidos(partido_recode_id = 80, all_pages = TRUE)

# Usar clave explícita solo para esta llamada
get_partidos(siglas = "psoe", api_key = "OTRA_CLAVE")
}
}
