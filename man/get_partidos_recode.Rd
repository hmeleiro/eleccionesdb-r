% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/partidos.R
\name{get_partidos_recode}
\alias{get_partidos_recode}
\title{List party recode/groups with optional filter}
\usage{
get_partidos_recode(
  agrupacion = NULL,
  limit = 50L,
  skip = 0L,
  all_pages = FALSE
)
}
\arguments{
\item{agrupacion}{Character. Partial search by grouping name,
case-insensitive (e.g. \code{"PCE/IU"}). Optional.}

\item{limit}{Integer. Maximum records per page (1-500, default 50).}

\item{skip}{Integer. Records to skip (default 0).}

\item{all_pages}{Logical. If \code{TRUE}, fetches all pages. Default \code{FALSE}.}
}
\value{
A tibble with columns: \code{id}, \code{partido_recode}, \code{agrupacion}, \code{color}.
}
\description{
Returns a paginated list of party recodes (groupings/families).
}
\examples{
\dontrun{
# All recode groups
get_partidos_recode(all_pages = TRUE)

# Search by grouping
get_partidos_recode(agrupacion = "PCE/IU")
}
}
