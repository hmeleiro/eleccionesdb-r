% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resultados.R
\name{get_resultado_completo}
\alias{get_resultado_completo}
\title{Get complete result for an election and territory}
\usage{
get_resultado_completo(
  eleccion_id,
  territorio_id,
  ...,
  denormalize = FALSE,
  use_recode = FALSE,
  clean = denormalize
)
}
\arguments{
\item{eleccion_id}{Integer. The election ID.}

\item{territorio_id}{Integer. The territory ID.}

\item{...}{Arguments after \code{...} must be named.}

\item{denormalize}{Logical. If \code{TRUE}, adds descriptive columns next to
ID columns: \code{eleccion_descripcion}, \code{territorio_nombre} (in both
\code{totales_territorio} and \code{votos_partido}), and \code{partido_nombre}
(in \code{votos_partido}). Requires additional API calls. Default \code{FALSE}.}

\item{use_recode}{Logical. If \code{TRUE} and \code{denormalize = TRUE}, the
\code{partido_nombre} column uses the recode group name (\code{agrupacion})
instead of the party abbreviation (\code{siglas}). Falls back to \code{siglas}
when the party has no recode. Default \code{FALSE}.}

\item{clean}{Logical. If \code{TRUE}, removes ID and slug columns from both
sub-tibbles. Defaults to the value of \code{denormalize}.}
}
\value{
A named list with two elements:
\describe{
\item{\code{totales_territorio}}{A 1-row tibble with summary fields: \code{id},
\code{eleccion_id}, \code{territorio_id}, \code{censo_ine}, \code{participacion_1},
\code{participacion_2}, \code{participacion_3}, \code{votos_validos},
\code{abstenciones}, \code{votos_blancos}, \code{votos_nulos}, \code{nrepresentantes}.}
\item{\code{votos_partido}}{A tibble of per-party votes, sorted by votes
descending. Columns: \code{id}, \code{eleccion_id}, \code{territorio_id},
\code{partido_id}, \code{votos}, \code{representantes}, \code{partido_id},
\code{partido_siglas}, \code{partido_denominacion},
\code{partido_partido_recode_id}.}
}
When \code{denormalize = TRUE}, additional columns \code{eleccion_descripcion},
\code{territorio_nombre}, and \code{partido_nombre} (in \code{votos_partido}) are inserted.
When \code{clean = TRUE}, ID and slug columns are removed.
}
\description{
Returns the territorial summary and per-party vote breakdown for a
specific election-territory combination. This is a composite endpoint
that returns a named list.
}
\details{
Party information is flattened into the votes tibble with prefixed
columns (\code{partido_id}, \code{partido_siglas}, \code{partido_denominacion},
\code{partido_partido_recode_id}).
}
\examples{
\dontrun{
result <- get_resultado_completo(208, 20)
result$totales_territorio
result$votos_partido

# With recode-based party names
result <- get_resultado_completo(208, 20,
    denormalize = TRUE, use_recode = TRUE
)
}
}
